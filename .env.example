# ============================================================
# 环境变量模板
# 使用方法：复制本文件为 .env，填写下方变量后再启动
#   Linux/macOS: cp .env.example .env
#   Windows:     Copy-Item .env.example .env
# .env 已被 .gitignore 忽略，不会提交到仓库。
# 注意：值不要加引号；文件保存为 UTF-8/ASCII 编码。
# ============================================================

# JWT 签名密钥（必填；留空启动会直接报错）
# 任意长度均可（内部经 SHA-256 派生为 256 位签名密钥），但安全强度取决于该值的随机性，
# 生产环境强烈建议使用 32 字符以上的随机字符串。生成方法：
#   Linux/macOS: openssl rand -base64 48
#   PowerShell:  [Convert]::ToBase64String((1..48 | ForEach-Object { Get-Random -Maximum 256 }))
# 提示：更换密钥会使所有已登录用户的 token 失效
JWT_SECRET=

# 管理员初始密码（可选，仅首次启动创建 admin 账号时生效）
# 不设置则使用内置默认值 admin123，并强制首次登录修改密码
ADMIN_INITIAL_PASSWORD=
